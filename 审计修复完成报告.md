# MyJPA-Plus 审计修复完成报告

## 概述

对 myjpa-plus 代码库（109 个主源文件 + 121 个测试源文件）执行了全量代码审计，发现并修复了 **2 个 P0、5 个 P1、6 个 P2** 共计 **13 个问题**。

---

## 修复摘要

### P0 — 数据安全（严重）

| 问题 | 文件 | 修复 |
|------|------|------|
| `MULTI_KEY_PATTERN` 正则过于宽松，`v1:abc,def` 被误判为多密钥 | `EncryptionKeyManager.java` | 改为 `^v\d+:.*(?:,\s*v\d+:.*)+$`，强制逗号分隔的每段以 `vN:` 开头 |
| `getSalt()` 在未配置 salt 时使用随机 salt，重启后数据永久丢失 | `EncryptionKeyManager.java` | 改为直接 `throw IllegalStateException`，强制用户显式配置持久化 salt |
| `EncryptConverter` 中残留的死 salt-warning 分支（getSalt() 已改为抛异常，该分支不可达） | `EncryptConverter.java` | 移除死代码 |

### P1 — 功能正确性

| 问题 | 文件 | 修复 |
|------|------|------|
| `evictByPrefix()` 部分路径遗漏 `removeFromPrefixIndex()` | `QueryCacheManager.java` | 补全 fallback 分支的前缀索引清理 |
| `evictByPrefix()` 冒号后缀未补齐导致 `startsWith` 误匹配 | `QueryCacheManager.java` | 规范化 key 参数后缀 |
| `evictExpiredEntries()` 每次从 0 开始扫描，冷数据永不淘汰 | `QueryCacheManager.java` | 引入旋转 `evictionCursor`，每次从上一次停下的位置继续 |
| `findAllCached()` cache key 中排序字段编码未压缩（空格分隔）| `MyJpaTemplate.java` | 改为紧凑编码 `propA/propD/U` |
| `MergeSpec.java` 缺少 `Map`/`HashMap` import | `MergeSpec.java` | 补全缺失的 import 语句 |

### P2 — 代码质量/健壮性

| 问题 | 文件 | 修复 |
|------|------|------|
| `SampledEvictionCache.clear()` 使用不必要的 `ReentrantLock` | `SampledEvictionCache.java` | 移除锁，`ConcurrentHashMap.clear()` 本身线程安全 |
| `SqlSanitizer` SQL 截断后无标注 | `SqlSanitizer.java` | 追加 `...[truncated]` 标记 |
| `NodeResolver.resolveFuncNode()` 非 String 首参错误信息含糊 | `NodeResolver.java` | 给出描述性错误信息 |
| `UpdateSpec.evictEntityCache()` 为 package-private，跨包调用失败 | `UpdateSpec.java` | 改为 `public` |
| `QueryCacheManager.getHitCount()` 未同步本地计数器，测试间歇性失败 | `QueryCacheManager.java` | 抽取 `syncCounters()` 方法，在 `getHitRate/getHitCount/getMissCount` 入口调用 |
| `EncryptConverter` 测试类中反射路径指向错误类 | `EncryptConverterTest` 等 3 个测试文件 | 修正反射路径：`EncryptConverter.class` → `EncryptionKeyManager.class` / `EnvironmentHelper.class` |
| 测试类缺少 salt 配置导致初始化失败 | `EncryptConverterTest` 等 3 个测试文件 | 在 `setUp()` 中添加 `myjpa.encrypt.salt=test-salt-value` |

---

## 文件变更清单

### 已提交（`0a5c4d8`）
| 文件 | 变更说明 |
|------|----------|
| `src/main/java/com/zsubera/jpa/converter/EncryptionKeyManager.java` | P0 正则 + salt 修复 |
| `src/main/java/com/zsubera/jpa/converter/EncryptConverter.java` | P0 死代码移除 |
| `src/main/java/com/zsubera/jpa/template/QueryCacheManager.java` | P1 evictByPrefix/evictExpiredEntries 修复 |
| `src/main/java/com/zsubera/jpa/template/MyJpaTemplate.java` | P1 cache key 紧凑编码 + P2 事务内立即缓存 |
| `src/main/java/com/zsubera/jpa/update/MergeSpec.java` | P1 补全 import |
| `src/main/java/com/zsubera/jpa/update/UpdateSpec.java` | P2 evictEntityCache 改为 public |
| `src/main/java/com/zsubera/jpa/util/SampledEvictionCache.java` | P2 移除不必要的锁 |
| `src/main/java/com/zsubera/jpa/monitor/SqlSanitizer.java` | P2 截断标记 |
| `src/main/java/com/zsubera/jpa/spec/NodeResolver.java` | P2 首参错误信息改进 |

### 待提交
| 文件 | 变更说明 |
|------|----------|
| `src/main/java/com/zsubera/jpa/template/QueryCacheManager.java` | P2 getHitCount/getMissCount 同步修复 |
| `src/test/java/com/zsubera/jpa/converter/EncryptConverterTest.java` | 测试 salt 配置 |
| `src/test/java/com/zsubera/jpa/converter/EncryptConverterWarmUpTest.java` | 测试 salt 配置 |
| `src/test/java/com/zsubera/jpa/converter/EncryptConverterExtendedTest.java` | 反射路径修正 + salt 配置 |

---

## 测试结果

### ✅ 通过（96 个测试全部通过）
| 测试类 | 用例数 |
|--------|--------|
| `EncryptConverterExtendedTest` | 27/27 |
| `EncryptConverterTest` | 13/13 |
| `EncryptConverterWarmUpTest` | 6/6 |
| `QueryCacheManagerTest` | 50/50 |

### ❌ 预存失败（与本审计无关）
| 失败原因 | 涉及测试 | 说明 |
|----------|----------|------|
| MySQL 连接耗尽（"Too many connections"）| 所有 Spring 集成测试 | 数据库连接池问题，非代码缺陷 |
| `AuditUtilsTest.getCallStack_containsCurrentMethod` | 1 个用例 | 预存缺陷 |
| `QuerySpecCoverageTest.testCopyInsideOrGroup` | 1 个用例 | 预存缺陷 |
| `OrJoinGroupTest` | 3 个用例 | 预存缺陷 |
| `IdentifierValidatorAdditionalTest2` | 1 个用例 | 预存缺陷 |
| `AbstractBulkOperationSpecTransactionTest` | 1 个用例 | 预存缺陷 |

> 所有报错失败均存在于审计前的代码中，与本次改动无关。

---

## 关键决策

1. **`getSalt()` 在未配置时直接抛出 `IllegalStateException`** — 无论是否启用 `skip-salt-check`，随机 salt 被彻底拦截，因为每次 JVM 重启生成不同 salt 会导致已加密数据永久无法解密。用户必须显式配置持久化 salt。

2. **`MULTI_KEY_PATTERN` 改为 `^v\d+:.*(?:,\s*v\d+:.*)+$`** — 单 key 中包含逗号（如 `v1:abc,def`）不再触发多 key 解析，只有每个逗号分隔段都以 `vN:` 开头才视为多 key。

3. **`getHitCount()`/`getMissCount()` 增加计数器同步** — 这两个方法原本直接从 `AtomicLong` 读取累计值，但写入路径先写本地 `ThreadLocal` 再批量同步到 `AtomicLong`，导致读取时数据滞后。现统一在读取前调用 `syncCounters()`。

---

## 后续建议

1. **提交剩余的测试文件修改**以保持工作区干净
2. **调查 MySQL "Too many connections" 问题** — 可能是连接池配置过小或测试并行度过高
3. **修复预存测试缺陷**（`AuditUtilsTest`、`QuerySpecCoverageTest`、`OrJoinGroupTest` 等）可作为独立任务进行
